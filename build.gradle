plugins {
    id 'java'
}

group 'com.nov0cx'
version '1.0.0'

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url 'https://jitpack.io'
    }
    maven {
        url = "https://nexus.funkemunky.cc/content/repositories/releases/"
    }
    maven {
        url = 'https://oss.sonatype.org/content/repositories/snapshots'
    }
    maven {
        url = 'https://oss.sonatype.org/content/repositories/releases/'
    }
}

dependencies {
    compileOnly "org.github.spigot:1.8.8:1.8.8"
    compileOnly "com.intellij:annotations:12.0"
    compileOnly "org.projectlombok:lombok:1.18.2"
    annotationProcessor 'org.projectlombok:lombok:1.18.2'
    compileOnly "cc.funkemunky.utils:java-rt:8"
    compileOnly "org.reflections:reflections:0.9.12"
}

task exportJarToBin(type: Copy, dependsOn: 'jar') {
    from jar
    into 'C:/Users/nikla/Desktop/server/plugins/'
}